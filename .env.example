# Browser-facing API prefix. The Vite development server proxies this to FastAPI.
VITE_API_BASE_URL=/api

# Local FastAPI address used by the Vite development proxy.
VITE_FASTAPI_TARGET=http://localhost:8000
